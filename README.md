# ENV needs
```
xCode
node
```
# build instructions
```
npm install
cd ios
pod install
cd ..
react-native start

<set team and certs in xcode>

<click play in xcode>
```

# APP ONLY RUNS ON DEVICES, NO SIMULATOR
